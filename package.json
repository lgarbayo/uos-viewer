{
  "name": "uos-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Visor de referencia del formato Unified Oral Scene (UOS). Layer 1: visualizacion y medicion manual, sin inferencia.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/three": "^0.185.1",
    "typescript": "^5.7.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
